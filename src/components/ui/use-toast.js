// src/components/ui/use-toast.js
export function toast({ title, description, variant }) {
    // For now, you could simply use an alert or console.log.
    // In a real app, you might use a Snackbar from MUI or another notification system.
    console.log(`${title}\n${description}`);
  }
  